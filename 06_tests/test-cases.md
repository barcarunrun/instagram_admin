# テストケース

## テストケーステンプレート

```
TC-001: [テストケース名]

前提条件:
  - ユーザーがログイン済み
  - ユーザーが投稿ページに存在

テスト手順:
  1. [操作1]
  2. [操作2]
  3. [操作3]

期待結果:
  - [期待される結果1]
  - [期待される結果2]

関連要件:
  - [01_requirements/...](01_requirements/...)

関連ストーリー:
  - As a [ユーザー] I want to [機能] So that [価値]
```

## ユーザー登録機能

### TC-001: 有効なメールアドレスでの登録

前提条件:

- 登録ページが開かれている
- ユーザーがまだ存在しない

テスト手順:

1. メールアドレス入力フィールドに「test@example.com」を入力
2. パスワード入力フィールドに「Password123!」を入力
3. 登録ボタンをクリック

期待結果:

- ユーザーが正常に登録される
- ホームページにリダイレクト
- 登録完了メッセージが表示される

### TC-002: 無効なメールアドレスでの登録失敗

前提条件:

- 登録ページが開かれている

テスト手順:

1. メールアドレス入力フィールドに「invalid-email」を入力
2. パスワード入力フィールドに「Password123!」を入力
3. 登録ボタンをクリック

期待結果:

- エラーメッセージが表示される
- ユーザーが登録されない
- ページが登録ページのままである

## 投稿機能

### TC-003: 投稿の作成と表示

前提条件:

- ユーザーがログイン済み
- 投稿ページが開かれている

テスト手順:

1. テキストエリアに「このテスト投稿です」と入力
2. 投稿ボタンをクリック

期待結果:

- 投稿が正常に作成される
- 投稿が投稿フィードに表示される
- 作成時刻が表示される

## Instagramアカウント連携

### TC-101: Instagramアカウント連携成功

前提条件:

- ユーザーがログイン済み
- connect 画面が開かれている
- OAuth モードが mock または real で有効になっている

テスト手順:

1. 「Facebookで連携する」をクリック
2. OAuth callback 完了後、候補アカウントを確認
3. 「このアカウントで接続」をクリック

期待結果:

- Instagramアカウントが active 状態で保存される
- 権限一覧に content_publish と pages_show_list が表示される
- connect 画面の連携状態カードが active に更新される

関連要件:

- [01_requirements/functional-requirements.md](01_requirements/functional-requirements.md)
- [01_requirements/acceptance-criteria.md](01_requirements/acceptance-criteria.md)

### TC-102: 権限不足アカウントの検出

前提条件:

- ユーザーがログイン済み
- mock シナリオ permission_denied が利用可能

テスト手順:

1. OAuth 開始を permission_denied シナリオで実行
2. callback 後の候補アカウントを確認
3. 接続確定を実行する

期待結果:

- 連携状態が reauthorization_required になる
- ダッシュボード alerts に要再認可アラートが出る
- 再認可導線が利用可能である

### TC-103: トークン期限切れの状態遷移

前提条件:

- token_expires_at が現在時刻以前の連携レコードがある

テスト手順:

1. connect 画面または dashboard alerts を開く
2. 連携状態取得 API を呼び出す

期待結果:

- 連携状態が expired に更新される
- 要再認可通知イベントが発火する
- 予約バリデーションで連携エラーになる

### TC-104: 再認可後の復旧

前提条件:

- 連携状態が expired または reauthorization_required である

テスト手順:

1. 「再認可する」をクリック
2. OAuth callback 完了後、対象アカウントを選択して保存

期待結果:

- status が active に復帰する
- tokenExpiresAt が新しい日時に更新される
- connect 画面で再認可完了メッセージが表示される

## 投稿コンテンツ管理

### TC-201: 画像アップロード付き下書き保存

前提条件:

- ユーザーがログイン済み
- contents 画面が開かれている

テスト手順:

1. 新規投稿作成を開始する
2. 有効な画像ファイルをアップロードする
3. 投稿名とキャプションを入力して下書きを保存する

期待結果:

- メディア資産が登録される
- 下書き一覧に新しいコンテンツが追加される
- バリデーション結果が問題なしで保存される

### TC-202: 動画メディアでの下書き保存

前提条件:

- ユーザーがログイン済み
- 利用可能な動画メディア資産が存在する

テスト手順:

1. 新規投稿作成を開始する
2. 投稿種別を動画に変更する
3. 動画メディアを選択して下書きを保存する

期待結果:

- 動画メディア付き下書きが保存される
- バリデーション結果が問題なしで保存される

### TC-203: 不正メディアの検出

前提条件:

- ユーザーがログイン済み

テスト手順:

1. サポート外のファイル形式をアップロードする

期待結果:

- アップロード API がエラーを返す
- UI に不正メディアのエラーメッセージが表示される

### TC-204: 下書き複製

前提条件:

- 既存の下書きが存在する

テスト手順:

1. 既存下書きを開く
2. 複製操作を実行する

期待結果:

- 元のメディア参照を維持した複製下書きが作成される
- タイトルに copy サフィックスが付与される

### TC-205: 更新履歴保存

前提条件:

- 既存の下書きが存在する

テスト手順:

1. 下書きのキャプションを更新する
2. 保存後に contents API を確認する

期待結果:

- content_versions に更新履歴が追加される
- summary に下書き更新が記録される

### TC-206: カルーセル順序付き下書き保存

前提条件:

- ユーザーがログイン済み
- 利用可能な画像メディア資産が 2 件以上存在する

テスト手順:

### TC-209: メディア管理ページで一覧とアップロードを確認できる

前提条件:

- ユーザーがログイン済み
- media 画面が開ける

テスト手順:

1. サイドバーからメディア管理画面を開く
2. 画像ファイルをアップロードする
3. 一覧にアップロード済みメディアが表示されることを確認する

期待結果:

- アップロード成功メッセージが表示される
- 一覧カードにファイル名と利用状況が表示される
- 詳細パネルにプレビューとメタデータが表示される

### TC-210: 未使用メディアを削除できる

前提条件:

- ユーザーがログイン済み
- 未使用のメディア資産が存在する

テスト手順:

1. メディア管理画面を開く
2. 未使用メディアを選択する
3. 削除操作を実行する

期待結果:

- 削除完了メッセージが表示される
- 対象メディアが一覧から消える

### TC-211: 利用中メディアは削除できない

前提条件:

- ユーザーがログイン済み
- 既存コンテンツで利用中のメディア資産が存在する

テスト手順:

1. メディア管理画面を開く
2. 利用中メディアを選択する
3. 削除ボタンの状態と削除 API の応答を確認する

期待結果:

- 削除ボタンが無効化される
- 削除 API は競合エラーを返す

## 予約投稿管理

### TC-301: 未来時刻で予約登録できる

前提条件:

- ユーザーがログイン済み
- approved 状態のコンテンツが存在する
- active 状態の Instagram 連携が存在する

テスト手順:

1. コンテンツ編集画面で未来日時を入力する
2. 「この日時で予約する」を実行する

期待結果:

- 予約作成成功メッセージが表示される
- 現在の予約情報が表示される
- 予約状態が「予約済み」になる

### TC-302: 過去日時での予約は拒否される

前提条件:

- ユーザーがログイン済み
- approved 状態のコンテンツが存在する

テスト手順:

1. コンテンツ編集画面で現在時刻より前の日時を入力する
2. 「この日時で予約する」を実行する

期待結果:

- 「公開日時は現在より後の時刻を指定してください。」が表示される
- 予約は作成されない

### TC-303: 同一コンテンツ・同一時刻の重複予約は拒否される

前提条件:

- ユーザーがログイン済み
- 対象コンテンツに同一公開先・同一時刻の予約が既に存在する

テスト手順:

1. 同じ contentId、accountId、publishAt で予約作成を再試行する

期待結果:

- API が 409 CONFLICT を返す
- エラーメッセージに重複予約不可が含まれる

### TC-304: 予約を取り消せる

前提条件:

- ユーザーがログイン済み
- 対象コンテンツに予約が存在する

テスト手順:

1. コンテンツ編集画面で現在の予約情報を表示する
2. 「予約を取り消す」を実行し、確認ダイアログを承認する

期待結果:

- 予約取消メッセージが表示される
- 現在の予約情報が消える
- 再度予約作成ボタンが利用可能になる

### TC-305: 予約済みスケジュールを更新できる

前提条件:

- ユーザーがログイン済み
- scheduled 状態の予約が存在する

テスト手順:

1. コンテンツ編集画面で公開日時を新しい未来時刻へ変更する
2. 「この内容で予約を更新」を実行する

期待結果:

- 予約更新メッセージが表示される
- 最新予約 API の publishAt が更新される

1. 新規投稿作成を開始する
2. 投稿種別をカルーセルに変更する
3. 画像メディアを 2 件以上選択する
4. 並び順を変更して下書きを保存する

期待結果:

- カルーセル下書きが保存される
- contentConfig.orderedMediaAssetIds に並び順が保持される

### TC-207: リールのカバー画像必須チェック

前提条件:

- ユーザーがログイン済み
- 利用可能な動画メディア資産と画像メディア資産が存在する

テスト手順:

1. 新規投稿作成を開始する
2. 投稿種別をリールに変更する
3. 動画のみを選択して保存する
4. カバー画像を指定して再保存する

期待結果:

- カバー画像未指定時は validation にエラーメッセージが表示される
- カバー画像指定後は validation が問題なしになる

### TC-208: 拡張種別テンプレート保持

前提条件:

- ユーザーがログイン済み
- 利用可能な画像メディア資産が存在する

テスト手順:

1. 新規投稿作成を開始する
2. 投稿種別を拡張に変更する
3. テンプレートキーを入力して下書きを保存する

期待結果:

- 拡張種別下書きが保存される
- contentConfig.templateKey に入力した値が保持される
